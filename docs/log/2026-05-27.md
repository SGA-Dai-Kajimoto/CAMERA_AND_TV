# 試行ログ 2026-05-27

---

Did: docs/ フォルダの重複ファイルを確認し、python_env_setup.md と README_for_agent.md を削除した
Got: TASKS.md に同等の内容が含まれており、問題なく整理できた
Next: TASKS.md を実際のタスク進行に合わせて都度更新する

---

Did: TASKS.md を旧内容から新しいタスク構成（API疎通/CUI/GUI）に書き直した
Got: 空のチェックボックスで整理されたクリーンな状態になった
Next: 各項目を実装・確認したら都度チェックを入れる

---

Did: api_spec_summary.md を公式ドキュメント（AccountPF / CommonLib）と照合した
Got: 記載済みエンドポイントに誤りはなかったが、いくつか不足が判明した（user/props、terms_of_use_status、clientlogs、actionlogs 等）
Next: 不足エンドポイントを追記済み。今後API呼び出しを実装する際は公式ドキュメントも参照する

---

---

Did: gui_app.py の重複コードを削除し、エントリーポイントのみに整理した
Got: GUI 起動が正常に動作することを確認
Next: 各API呼び出しの整備に続く

---

Did: `POST /api/v1/folders` の 400 エラーを修正した（`app_id`・`utc_offset` リクエストボディに追加）
Got: フォルダ作成が成功するようになった
Next: コンテンツ操作の修正へ

---

Did: `delete_content` を `DELETE` から `POST .../contents:remove` に修正した
Got: 正しいエンドポイントで削除が成功するようになった
Next: アップロードフローの修正へ

---

Did: `upload_image` のアップロードエンドポイントを修正し（ステップ1・ステップ3の正しいパスへ変更）、425 エラー対策に `wait_time=10s` クエリパラメータとリトライループを追加した
Got: 画像アップロードが成功。425 エラーも解消された
Next: コンテンツ表示機能の追加

---

Did: `GET /api/v1/folders` に削除リクエスト状態のフォルダが返ってくる問題を修正。`removed_date` があるフォルダを `list_folders` で除外するようにした
Got: API履行中の削除は即時屡行ではなく「削除リクエスト」状態（`removed_date`付与）を彼ることが判明。その間フォルダ一覧に表示されのコンテンツ取得は404になる
Next: コンテンツ対も同様の状態遷移がある可能性あり。必要なら `list_contents` 側でもフィルタリングを追加する

---
